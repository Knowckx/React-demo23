
// 500句搞定JS语法


// 打印， 输出到浏览器的控制台
console.log("222") 

// 输出一个格式化的字符串
let myName = "knowckx";
let info = `My Name is ${myName}`
console.log(info);


// 定位变量
let a = 1 // 定义局部变量 可变
const aa = 2 // 无法改变

// 支持三元表达式
squares[i] = this.state.xIsNext ? 'X' : 'O';


// 小数位
res = res.toFixed(2)

// ===——严格相等（三个等号）  == 宽松相等


// node相关的
// 预先定义的全局变量
console.log(process);
console.log(process.env.NODE_ENV);  // 

